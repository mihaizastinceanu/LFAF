public enum TokenType {
    // Keywords
    SIN, COS,

    // Identifiers
    IDENTIFIER,

    // Numbers
    INTEGER,
    FLOAT,

    // Operators
    PLUS, MINUS, MULTIPLY, DIVIDE, ASSIGN,

    // Parentheses
    LPAREN, RPAREN,

    // Special
    EOF
}